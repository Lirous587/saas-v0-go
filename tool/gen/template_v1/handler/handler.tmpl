package handler

import (
	"github.com/gin-gonic/gin"
	"github.com/pkg/errors"
    "strconv"
	"{{.Module}}/internal/common/reqkit/bind"
	"{{.Module}}/internal/common/reskit/response"
	"{{.Module}}/internal/{{.Domain}}/domain"
)

type HttpHandler struct {
    service domain.{{.DomainTitle}}Service
}

func NewHttpHandler(service domain.{{.DomainTitle}}Service) *HttpHandler {
    return &HttpHandler{
        service: service,
    }
}


// Create godoc
// @Summary      创建
// @Tags         {{.Domain}}
// @Accept       json
// @Produce      json
// @Security     BearerAuth
// @Param        request body handler.CreateRequest true "请求参数"
// @Success      200  {object}  response.successResponse "请求成功"
// @Failure      400  {object}  response.invalidParamsResponse "参数错误"
// @Failure      500  {object}  response.errorResponse "服务器错误"
// @Router       /v1/{{.Domain}} [post]
func (h *HttpHandler) Create(ctx *gin.Context) {
    req := new(CreateRequest)

	if err := bind.BindingRegularAndResponse(ctx,req); err != nil {
		return
	}

    if err := h.service.Create(&domain.{{.DomainTitle}}{
        Title:    req.Title,
        Description:  req.Description,
    });err != nil {
        response.Error(ctx, err)
        return
    }

    response.Success(ctx)
}

// Update godoc
// @Summary      更新
// @Tags         {{.Domain}}
// @Accept       json
// @Produce      json
// @Security     BearerAuth
// @Param        id   path string true "id"
// @Param        request body handler.UpdateRequest true "请求参数"
// @Success      200  {object}  response.successResponse "请求成功"
// @Failure      400  {object}  response.invalidParamsResponse "参数错误"
// @Failure      500  {object}  response.errorResponse "服务器错误"
// @Router       /v1/{{.Domain}}/{id} [put]
func (h *HttpHandler) Update(ctx *gin.Context) {
    req := new(UpdateRequest)

	if err := bind.BindingRegularAndResponse(ctx,req); err != nil {
		return
	}

    err := h.service.Update(&domain.{{.DomainTitle}}{
        ID:           req.ID,
        Title:        req.Title,
        Description:  req.Description,
    })

    if err != nil {
        response.Error(ctx, err)
        return
    }

    response.Success(ctx)
}

// Delete godoc
// @Summary      删除
// @Tags         {{.Domain}}
// @Accept       json
// @Produce      json
// @Security     BearerAuth
// @Param        id   path string true "id"
// @Success      200  {object}  response.successResponse "请求成功"
// @Failure      400  {object}  response.invalidParamsResponse "参数错误"
// @Failure      500  {object}  response.errorResponse "服务器错误"
// @Router       /v1/{{.Domain}}/{id} [delete]
func (h *HttpHandler) Delete(ctx *gin.Context) {
    req := new(DeleteRequest)

	if err := bind.BindingRegularAndResponse(ctx,req); err != nil {
		return
	}

    if err := h.service.Delete(req.ID); err != nil {
        response.Error(ctx, err)
        return
    }

    response.Success(ctx)
}

// Read godoc
// @Summary      读取单条数据
// @Tags         {{.Domain}}
// @Accept       json
// @Produce      json
// @Param        id   path string true "id"
// @Success      200  {object}  response.successResponse{data=handler.{{.DomainTitle}}Response} "请求成功"
// @Failure      400  {object}  response.invalidParamsResponse "参数错误"
// @Failure      500  {object}  response.errorResponse "服务器错误"
// @Router       /v1/{{.Domain}}/{id} [get]
func (h *HttpHandler) Read(ctx *gin.Context) {
    req := new(ReadRequest)

	if err := bind.BindingRegularAndResponse(ctx,req); err != nil {
		return
	}

	data, err := h.service.Read(req.ID)

	if err != nil {
		response.Error(ctx, err)
		return
	}

	response.Success(ctx, domain{{.DomainTitle}}ToResponse(data))
}

// List godoc
// @Summary      获取列表
// @Tags         {{.Domain}}
// @Accept       json
// @Produce      json
// @Param        keyword    query     string  false  "关键词"
// @Param        page       query     int     false  "页号"
// @Param        page_size  query     int     false  "页码"
// @Success      200  {object}  response.successResponse{data=handler.{{.DomainTitle}}ListResponse} "请求成功"
// @Failure      400  {object}  response.invalidParamsResponse "参数错误"
// @Failure      500  {object}  response.errorResponse "服务器错误"
// @Router       /v1/{{.Domain}} [get]
func (h *HttpHandler) List(ctx *gin.Context) {
    req := new(ListRequest)

	if err := bind.BindingRegularAndResponse(ctx,req); err != nil {
		return
	}

    data, err := h.service.List(&domain.{{.DomainTitle}}Query{
        Keyword:  req.KeyWord,
        Page:     req.Page,
        PageSize: req.PageSize,
    })

    if err != nil {
        response.Error(ctx, err)
        return
    }

    response.Success(ctx, domain{{.DomainTitle}}ListToResponse(data))
}